# Repository URLs that return JSON arrays of addon entries with:
# name, description, author, item, download (.jar link)
repositories:
  - https://raw.githubusercontent.com/UtilityXCoding/Repository/refs/heads/main/addons.json
